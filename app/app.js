let app = angular.module("appName",[])

app.controller("appControllerName", function ($scope) {
    
    });

    angular.module(Lulz).run(function (FIREBASE_CONFIG) {
        firebase.initializeApp(FIREBASE_CONFIG)
    })